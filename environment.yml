name: lstio
channels:
  - conda-forge
  - default
dependencies:
  - astropy>=4.2
  - python=3.8 # nail the python version, so conda does not try upgrading / dowgrading
  - ctapipe=0.12
  - protozfits=2.2
  - eventio
  - corsikaio
  - zeromq
  - ipython
  - numba
  - numpy>=1.20,<1.23
  - numpydoc
  - pytest
  - pyyaml
  - zlib
  - pip
